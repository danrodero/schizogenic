spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:${PGPORT:5432}/${PGDATABASE:schizogenic_dev}}
    username: ${SPRING_DATASOURCE_USERNAME:${PGUSER:schizogenic}}
    password: ${SPRING_DATASOURCE_PASSWORD:}
  flyway:
    locations: "classpath:db/migration/postgresql"
